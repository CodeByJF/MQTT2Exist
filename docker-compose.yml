version: "3.8"

services:
  mqtt2exist:
    build: .
    container_name: mqtt2exist
    # Use host network so MQTT_HOST=localhost can hit your host's MQTT broker
    network_mode: host
    restart: unless-stopped
    # Optional: always run with unbuffered output for realtime logs
    command: ["python", "-u", "mqtt2exist.py"]